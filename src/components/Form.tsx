// Form.tsx

const Form = () => {
    return (
        <form>
            <input type="text" name="city" placeholder="都市名"/>
            <button type="submit">都市の天気予報を取得する。</button>
        </form>

    );
}

export default Form;